version: 2

sources:

  - name: PUBLIC # name of the source 
    schema: PUBLIC # this is the schema our raw data lives in
    database: ALUMNO21_DEV_BRONZE_DB # this is the name of our database

    tables:

      - name: USERS
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema